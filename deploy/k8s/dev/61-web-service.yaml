apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: pulsewatch
spec:
  selector: { app: web }
  ports:
    - name: http
      port: 5173
      targetPort: 5173
  type: ClusterIP
