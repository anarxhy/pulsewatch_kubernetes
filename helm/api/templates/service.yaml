apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: pulsewatch
spec:
  selector: { app: api }
  ports:
    - name: http
      port: {{ .Values.ports.http }}
      targetPort: {{ .Values.ports.http }}
  type: ClusterIP
